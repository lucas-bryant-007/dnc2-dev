# Frozen rerun references

These are the three corrected, post-audit metric JSONs and environment record
used by `analysis/run_paper_rerun_s2.sh` for numerical regression checks. They
are inputs to verification, not a separate figure release.
